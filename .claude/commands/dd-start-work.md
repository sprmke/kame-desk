# dd-start-work

Start work on GitHub issue #$ARGUMENTS.

1. `git fetch origin && git checkout main && git pull origin main`
2. `git checkout -b issue-$ARGUMENTS-short-slug`
3. Read the issue and matching `docs/phases/` file if applicable.
