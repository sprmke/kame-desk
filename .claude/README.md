# Claude Code — DoctorDesk

Mirrors `.cursor/` tooling. Index:

- **Skills:** `.agent/skills/` → `.claude/skills/`
- **Commands:** `.claude/commands/` (`/dd-*` + workflow/superpowers)
- **Agents:** `debugger`, `security-auditor`, `test-runner`, `verifier`, `design-reviewer`
- **Hooks:** `.claude/settings.json` + `.claude/hooks/`
- **MCP:** root `.mcp.json`

Setup: `pnpm run setup:ai-tooling` · Drift: `pnpm run check:ai-tooling-sync`
